// Code generated by goa v3.0.9, DO NOT EDIT.
//
// calc gRPC client CLI support package
//
// Command:
// $ goa gen github.com/mdetweil/demo-reverse-proxy/server/design

package cli

import (
	"flag"
	"fmt"
	"os"

	calcc "github.com/mdetweil/demo-reverse-proxy/server/gen/grpc/calc/client"
	worldc "github.com/mdetweil/demo-reverse-proxy/server/gen/grpc/world/client"
	goa "goa.design/goa/v3/pkg"
	grpc "google.golang.org/grpc"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `calc (add|multiply)
world hello
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` calc add --message '{
      "a": 360622074634248926,
      "b": 8133055152903002499
   }'` + "\n" +
		os.Args[0] + ` world hello` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(cc *grpc.ClientConn, opts ...grpc.CallOption) (goa.Endpoint, interface{}, error) {
	var (
		calcFlags = flag.NewFlagSet("calc", flag.ContinueOnError)

		calcAddFlags       = flag.NewFlagSet("add", flag.ExitOnError)
		calcAddMessageFlag = calcAddFlags.String("message", "", "")

		calcMultiplyFlags       = flag.NewFlagSet("multiply", flag.ExitOnError)
		calcMultiplyMessageFlag = calcMultiplyFlags.String("message", "", "")

		worldFlags = flag.NewFlagSet("world", flag.ContinueOnError)

		worldHelloFlags = flag.NewFlagSet("hello", flag.ExitOnError)
	)
	calcFlags.Usage = calcUsage
	calcAddFlags.Usage = calcAddUsage
	calcMultiplyFlags.Usage = calcMultiplyUsage

	worldFlags.Usage = worldUsage
	worldHelloFlags.Usage = worldHelloUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "calc":
			svcf = calcFlags
		case "world":
			svcf = worldFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "calc":
			switch epn {
			case "add":
				epf = calcAddFlags

			case "multiply":
				epf = calcMultiplyFlags

			}

		case "world":
			switch epn {
			case "hello":
				epf = worldHelloFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "calc":
			c := calcc.NewClient(cc, opts...)
			switch epn {
			case "add":
				endpoint = c.Add()
				data, err = calcc.BuildAddPayload(*calcAddMessageFlag)
			case "multiply":
				endpoint = c.Multiply()
				data, err = calcc.BuildMultiplyPayload(*calcMultiplyMessageFlag)
			}
		case "world":
			c := worldc.NewClient(cc, opts...)
			switch epn {
			case "hello":
				endpoint = c.Hello()
				data = nil
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// calcUsage displays the usage of the calc command and its subcommands.
func calcUsage() {
	fmt.Fprintf(os.Stderr, `The calc service performs operations on numbers
Usage:
    %s [globalflags] calc COMMAND [flags]

COMMAND:
    add: Add implements add.
    multiply: Multiply implements multiply.

Additional help:
    %s calc COMMAND --help
`, os.Args[0], os.Args[0])
}
func calcAddUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] calc add -message JSON

Add implements add.
    -message JSON: 

Example:
    `+os.Args[0]+` calc add --message '{
      "a": 360622074634248926,
      "b": 8133055152903002499
   }'
`, os.Args[0])
}

func calcMultiplyUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] calc multiply -message JSON

Multiply implements multiply.
    -message JSON: 

Example:
    `+os.Args[0]+` calc multiply --message '{
      "a": 5401762099778430809,
      "b": 1918630006328122782
   }'
`, os.Args[0])
}

// worldUsage displays the usage of the world command and its subcommands.
func worldUsage() {
	fmt.Fprintf(os.Stderr, `The world service returns hello world
Usage:
    %s [globalflags] world COMMAND [flags]

COMMAND:
    hello: Hello implements hello.

Additional help:
    %s world COMMAND --help
`, os.Args[0], os.Args[0])
}
func worldHelloUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] world hello

Hello implements hello.

Example:
    `+os.Args[0]+` world hello
`, os.Args[0])
}
